// "Rules" - Button displayed in the pre-game section
const Rules = ({ onClick }) => {
  return (
    <button className="btn btn-pregame" onClick={onClick}>
      Rules
    </button>
  );
};

export default Rules;
