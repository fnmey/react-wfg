// "About" - Button displayed in the pre-game section
const About = ({ onClick }) => {
  return (
    <button className="btn btn-pregame" onClick={onClick}>
      About
    </button>
  );
};

export default About;
