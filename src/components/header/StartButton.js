const StartButton = ({onClick}) => {
    return (
        <button 
        className="btn btn-start"
        onClick = {onClick}>
            Start Game!
        </button>
    )
}

export default StartButton
