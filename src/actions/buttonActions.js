// action on state "nextRoundButton"
export const toggle_NextRoundButton = () => {
    return {
        type: "TOGGLE_NEXT_ROUND_BUTTON"
    }
}

// action on state "endMoveButton"
export const toggle_EndMoveButton = () => {
    return {
        type: "TOGGLE_END_MOVE_BUTTON"
    }
}