// actions on state "moveCounter"
export const increment_MoveCounter = () => {
    return {
        type: "INCREMENT_MOVE_COUNTER"
    }
}
export const reset_MoveCounter = () => {
    return {
        type: "RESET_MOVE_COUNTER"
    }
}

// actions on state "roundCounter"
export const increment_RoundCounter = () => {
    return {
        type: "INCREMENT_ROUND_COUNTER"
    }
}

